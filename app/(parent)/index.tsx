import { Text } from 'react-native';

import { Card } from '@/components/ui/card';
import { Screen } from '@/components/ui/screen';

export default function ParentDashboardScreen() {
  return (
    <Screen>
      <Card>
        <Text>Parent portal dashboard scaffold</Text>
      </Card>
    </Screen>
  );
}
