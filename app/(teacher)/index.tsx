import { Text } from 'react-native';

import { Card } from '@/components/ui/card';
import { Screen } from '@/components/ui/screen';

export default function TeacherDashboardScreen() {
  return (
    <Screen>
      <Card>
        <Text>Teacher portal dashboard scaffold</Text>
      </Card>
    </Screen>
  );
}
