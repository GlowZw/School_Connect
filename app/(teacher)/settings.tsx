import { PlaceholderScreen } from '@/components/ui/placeholder-screen';

export default function TeacherSettingsScreen() {
  return (
    <PlaceholderScreen
      description="Manage teacher account preferences and portal settings from one place."
      eyebrow="Teacher Portal"
      highlights={[
        'Teacher profile settings entry point.',
        'Sidebar settings navigation added.',
        'Dashboard shortcut icon added.',
      ]}
      title="Settings"
    />
  );
}
