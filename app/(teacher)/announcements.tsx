import { PlaceholderScreen } from '@/components/ui/placeholder-screen';

export default function TeacherAnnouncementsScreen() {
  return (
    <PlaceholderScreen
      description="Create and manage class and school announcements for linked audiences."
      eyebrow="Teacher Portal"
      highlights={['Audience targeting follows school membership.', 'Notifications are delivered through the shared notification system.']}
      title="Announcements"
    />
  );
}
