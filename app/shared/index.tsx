import { Text } from 'react-native';

import { Card } from '@/components/ui/card';
import { Screen } from '@/components/ui/screen';

export default function SharedRouteScreen() {
  return (
    <Screen>
      <Card>
        <Text>Shared route placeholder</Text>
      </Card>
    </Screen>
  );
}
