import { PlaceholderScreen } from '@/components/ui/placeholder-screen';

export default function AdminCalendarScreen() {
  return (
    <PlaceholderScreen
      description="Coordinate school events, operational dates, and calendar-driven administration."
      eyebrow="Admin Portal"
      highlights={[
        'School-wide event scheduling and planning visibility.',
        'Administrative reminders and term calendar management.',
        'Calendar access added to the sidebar and dashboard.',
      ]}
      title="Calendar"
    />
  );
}
