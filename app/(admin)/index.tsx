import { Text } from 'react-native';

import { Card } from '@/components/ui/card';
import { Screen } from '@/components/ui/screen';

export default function AdminDashboardScreen() {
  return (
    <Screen>
      <Card>
        <Text>Admin portal dashboard scaffold</Text>
      </Card>
    </Screen>
  );
}
